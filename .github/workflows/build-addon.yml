name: Build Addon Docker Images

on:
  push:
    branches:
      - main
    paths:
      - 'trmnl-screenshot/**'
      - '.github/workflows/build-addon.yml'
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      arch:
        description: 'Architecture to build (amd64, armv7, aarch64, or all)'
        required: true
        default: 'amd64'

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: chbarnhouse

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: read
      packages: write

    strategy:
      max-parallel: 1
      matrix:
        include:
          - arch: amd64
            platforms: linux/amd64
          # ARM builds disabled temporarily - npm install issues
          # - arch: armv7
          #   platforms: linux/arm/v7
          # - arch: aarch64
          #   platforms: linux/arm64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: version
        run: |
          VERSION=$(grep '^version:' trmnl-screenshot/config.yaml | awk '{print $2}' | tr -d '"')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build and push ${{ matrix.arch }} image
        uses: docker/build-push-action@v5
        with:
          context: trmnl-screenshot
          platforms: ${{ matrix.platforms }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.arch }}-addon-trmnl-screenshot:${{ steps.version.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.arch }}-addon-trmnl-screenshot:latest
          build-args: |
            BUILD_FROM=node:20-slim
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Summary
        run: |
          echo "✅ Built and pushed ${{ matrix.arch }} image"
          echo "Image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}/${{ matrix.arch }}-addon-trmnl-screenshot:${{ steps.version.outputs.version }}"
