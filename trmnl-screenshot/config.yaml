---
name: TRMNL Screenshot
version: "0.1.0"
slug: trmnl_screenshot
description: Capture Home Assistant dashboards for TRMNL e-ink devices
url: "https://github.com/chbarnhouse/ha-addons/tree/main/trmnl-screenshot"
arch:
  - amd64
init: false
startup: services
options:
  screenshot_interval: 300
  image_quality: 90
  max_concurrent_captures: 3
  log_level: info
schema:
  screenshot_interval:
    type: integer
    description: Screenshot capture interval in seconds
    default: 300
    minimum: 60
    maximum: 3600
  image_quality:
    type: integer
    description: Image quality for PNG compression (1-100)
    default: 90
    minimum: 1
    maximum: 100
  max_concurrent_captures:
    type: integer
    description: Maximum concurrent screenshot captures
    default: 3
    minimum: 1
    maximum: 10
  log_level:
    type: list
    description: Logging level
    default: info
    options:
      - debug
      - info
      - warn
      - error
image: "ghcr.io/chbarnhouse/{arch}-addon-trmnl-screenshot"
